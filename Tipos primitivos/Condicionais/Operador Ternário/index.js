const velocidade = 86;
const warn = 85;

//condicao ? true : false

const condicao =
    velocidade >= warn
    ? console.log ("Recebeu uma multa!")
    : console.log ("Continue andando");

    if (velocidade >=warn) {
        console.log ("Recebeu uma multa!")
    }else {
        console.log ("Continue andando");
    }